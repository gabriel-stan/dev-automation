# ubuntu-java-stack-dev

This setup includes a Vagrantfile provisioned by shell scripts, which installs xfce4 gui, jdk8 and eclipse, with shared folders to the host machine in order to save the workspace.